extends layout-main

block content
  style.
    .main-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 20px; /* Espaço entre os elementos */
      margin-top: 100px; /* Ajustar conforme necessário para a distância do header */
    }
    .main-title {
      background-color: #1fd761; /* Cor de fundo da caixa */
      width: 350px;
      color: white;
      padding: 10px 20px;
      border-radius: 5px;
      margin-bottom: 20px;
      text-align: center;
    }
  .main-container
    .main-title
      h1 Página Principal
